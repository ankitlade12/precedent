{
  "name": "@precedent/llm-anthropic",
  "version": "0.1.0",
  "description": "Claude-backed decision detector: extracts a structured, grounded decision proposal from a thread via the Anthropic API. Everything it returns is still a proposal a human confirms.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@precedent/proposer": "^0.1.0",
    "zod": "^3.24.1"
  },
  "scripts": {
    "test": "vitest run"
  }
}
