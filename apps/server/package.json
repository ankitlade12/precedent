{
  "name": "@precedent/server",
  "version": "0.1.0",
  "private": true,
  "description": "Composition root: wires the ledger, model layer, Slack surface, and MCP server into one deployable process.",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@precedent/llm-anthropic": "^0.1.0",
    "@precedent/ledger-core": "^0.1.0",
    "@precedent/mcp-server": "^0.1.0",
    "@precedent/proposer": "^0.1.0",
    "@precedent/slack-app": "^0.1.0",
    "@precedent/store-sqlite": "^0.1.0",
    "dotenv": "^16.4.7"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run"
  }
}
